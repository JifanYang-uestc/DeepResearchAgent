# DeepResearchAgent V0.3 Development Summary

## 1. Compatibility Spike

开发基线为 `v0.2-knowledge-rag`，目标框架版本为
`hello-agents==0.2.9`。隔离 Spike 已验证：

- `from hello_agents.tools import RAGTool` 可用。
- HelloAgents 基础安装不包含本地语义检索所需的全部可选依赖；本方案只新增
  `sentence-transformers`。
- `LocalTransformerEmbedding` 可在 Windows/CPU 上运行，实测模型
  `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` 输出 384 维向量。
- `RAGTool.run()` 返回面向人的字符串，不适合恢复结构化 Citation metadata；
  lower-level pipeline 可返回结构化 `id/score/metadata`。
- HelloAgents 的 MarkItDown 路径不能保留 PDF 页码；内置 Qdrant adapter 默认要求
  `localhost:6333`，也未暴露本地 persistent path。
- 最终复用 HelloAgents 的本地 Transformer Embedding，同时保留项目已有的页码感知
  Loader/Chunker、FAISS 持久化和 `RetrievalResult` / `EvidenceSource` 模型。
- 未采用 Qdrant、DashScope、MQE、HyDE 或字符串解析。

完整记录见 `docs/v0.3-rag-compatibility.md`，可复现脚本为
`backend/scripts/v03_compatibility_spike.py`。

## 2. Architecture Before — V0.2

```text
User Query -> Planner -> TODO
                         |-> HashingEmbedding + FAISS
                         |-> Web Search
                         `-> Unified Evidence -> Summarizer -> Reporter -> SSE
```

每个 TODO 固定执行本地 Top-K 和 Web Search；Top-K 即使很弱也可能成为最终证据。

## 3. Architecture After — V0.3

```text
User -> Planner -> TODO -> Adaptive Retrieval Router
                            |-> Knowledge -> Semantic RAG --|
                            |-> Web ------------------------|-> Unified Evidence
                            `-> Hybrid -> Semantic + Web ---|      |
                                           |                      v
                                Knowledge Relevance Gate     Summarizer
                                                               |
                                                        Reporter -> SSE/Vue
```

`KnowledgeService` 仍是上层稳定边界。默认后端为 HelloAgents Semantic Backend，
V0.2 `HashingEmbedding + FAISS` 作为 `legacy_faiss` 保留。Router、Gate、Evidence
编排分别位于独立服务中，`agent.py` 只负责流程和事件协调。

## 4. Files

### Added

- `backend/src/rag/base.py`：Knowledge Backend Protocol 与 Catalog 类型。
- `backend/src/rag/catalog.py`：不加载 Embedding 的轻量文档目录。
- `backend/src/rag/helloagents_backend.py`：HelloAgents 本地语义向量适配与 FAISS 持久化。
- `backend/src/rag/legacy_faiss_backend.py`：V0.2 后端适配与回退。
- `backend/src/services/relevance_gate.py`：语义阈值 Gate 与 Legacy judge。
- `backend/src/services/retrieval_router.py`：typed route、确定性信号、结构化 LLM provider 与 fallback。
- `backend/scripts/v03_compatibility_spike.py`：HelloAgents 0.2.9 隔离兼容性实验。
- `backend/scripts/debug_semantic_retrieval.py`：显示分数、Gate、文档、页码和 Chunk ID。
- `backend/scripts/run_v03_acceptance.py`：凭据就绪后可运行的真实 SSE 审计脚本。
- `backend/pytest.ini`：注册隔离的 live test marker。
- `backend/tests/live/test_v03_semantic_cases.py`：本地模型、真实论文三路验收。
- `backend/tests/test_adaptive_research_evidence.py`：路由与故障降级编排测试。
- `backend/tests/test_helloagents_backend.py`：语义适配、metadata 与重载测试。
- `backend/tests/test_knowledge_backend.py`：Backend Protocol 与 Legacy 回归。
- `backend/tests/test_relevance_gate.py`：弱相关拒绝、域内接受与分数语义测试。
- `backend/tests/test_retrieval_router.py`：六查询矩阵、invalid JSON 和 timeout fallback。
- `backend/tests/test_router_out_of_domain.py`：机器人域外污染回归测试。
- `backend/tests/test_routing_observability.py`：新增 SSE 事件契约测试。
- `backend/tests/test_v03_end_to_end.py`：三路 TODO 到最终 `done` 的确定性 SSE E2E。
- `docs/v0.3-rag-compatibility.md`：Compatibility Spike 结论。
- `V0.3_DEVELOPMENT_SUMMARY.md`：本总结。

### Modified

- `README.md`、`backend/knowledge_base/README.md`：架构、环境、索引、Demo 与验收说明。
- `backend/.env.example`、`backend/src/config.py`：V0.3 统一配置。
- `backend/pyproject.toml`、`backend/uv.lock`：最小语义依赖和质量配置。
- `backend/scripts/build_knowledge_index.py`：通过 `KnowledgeService` 构建所选后端。
- `backend/src/agent.py`、`backend/src/models.py`、`backend/src/main.py`：路由协调、typed 状态、SSE 和 API 字段。
- `backend/src/rag/__init__.py`、`chunker.py`、`retriever.py`、`types.py`、`vector_store.py`：抽象兼容、类型与格式整理。
- `backend/src/services/knowledge.py`、`research.py`、`search.py`：后端生命周期、自适应 Evidence 与延迟初始化 Web client。
- `backend/src/services/planner.py`、`reporter.py`、`summarizer.py`、`backend/src/utils.py`：质量检查的安全格式整理。
- 既有 RAG 测试：保留断言并做 import/类型格式整理。
- `frontend/src/App.vue`：轻量展示 Route、原因、置信度和 Knowledge rejection。

### Deleted

- 无。

## 5. Dependencies

新增 `sentence-transformers>=6.0.0`，用于 HelloAgents 官方
`LocalTransformerEmbedding`。没有引入 `hello-agents[all]`、Qdrant、Neo4j、
Redis 或其它无关可选组件。开发组继续使用 pytest 与 Ruff。

## 6. Configuration

```env
KNOWLEDGE_BACKEND=helloagents
EMBEDDING_PROVIDER=local_transformer
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
ENABLE_RETRIEVAL_ROUTER=True
KNOWLEDGE_PROBE_TOP_K=3
KNOWLEDGE_RELEVANCE_THRESHOLD=0.55
ENABLE_ADVANCED_RAG_SEARCH=False
```

所有字段从 `Configuration.from_env()` 进入业务层。切换
`KNOWLEDGE_BACKEND=legacy_faiss` 可回归 V0.2 后端。

## 7. Routing Logic

- **Knowledge**：Catalog 直接覆盖稳定理论、指定论文或本地文档，且不要求当前信息。
- **Web**：Catalog 无直接资料，或问题需要当前新闻、市场、价格、融资等时效信息。
- **Hybrid**：本地论文能提供基础，同时任务要求最新互联网资料。

Router 输入包含研究主题、TODO 标题/意图/查询、当前日期和精简 Catalog。
确定性信号与结构化 LLM provider 共同工作；LLM timeout、异常或无效 JSON 时使用
确定性 fallback。Web-only 不触发 Knowledge，Knowledge-only 在本地证据有效时不触发 Web。

## 8. Relevance Gate

Semantic Backend 返回 cosine similarity 后，先取 probe Top-K，再用独立 Gate
逐项过滤，只有 Accepted Evidence 才能进入 Summarizer 和最终 Citation。
实际论文语料校准结果：

- 域内 Top-1：`0.6821–0.7398`
- 域外 Top-1：`0.3577–0.4198`
- 采用阈值：`0.55`

Legacy score 与 semantic cosine 不混用；Legacy Backend 使用独立轻量相关性判断。

## 9. Tests and Checks

| Check | Result | Detail |
|---|---|---|
| 后端快速测试 | PASS | 49 passed |
| 默认 live test | SKIP | 1 skipped，需显式 `RUN_SEMANTIC_LIVE=1` |
| 本地语义 live test | PASS | 1 passed；真实模型 + 本地 RAG/ReAct/Self-RAG PDF |
| 路由六查询矩阵 | PASS | Knowledge 2、Web 2、Hybrid 2 |
| 机器人域外回归 | PASS | Route=Web 且 Knowledge sources 为空 |
| Gate 域内/域外 | PASS | ReAct 接受；弱相关机器人候选拒绝 |
| 故障矩阵 | PASS | invalid JSON、timeout、Knowledge/Web/Embedding/Vector/Both failure |
| SSE 本地 E2E | PASS | 三类 Route、两类 Source、Final Report、最后事件 `done` |
| Ruff | PASS | `All checks passed!` |
| Vue/TypeScript production build | PASS | 14 modules transformed |
| 真实第三方 LLM/Tavily E2E | SKIP | 未取得将查询发送到第三方服务的明确授权 |

唯一 warning 来自 HelloAgents 0.2.9 内部 Pydantic V2 旧式 `Config`，不是项目测试失败。

## 10. Regression

输入：`机器人领域的发展趋势`，Catalog 为 RAG/ReAct/Self-RAG。

- V0.2：固定执行本地 Top-K，可能把 `react.pdf`、`self_rag.pdf`、
  `rag_2020.pdf` 的弱相关 Chunk 写进 Evidence。
- V0.3：Router 判定 `WEB`，Knowledge Retrieval 不执行；最终 Knowledge sources
  精确断言为空，Web Evidence 保留。

## 11. Demo Acceptance

### Knowledge-only

`ReAct 如何结合 reasoning 和 acting？`

- Route：Knowledge
- 本地真实检索 Top-1：`react.pdf`，page 2，score `0.787971`
- Chunk ID：`92a71d90a039f5aa10a1`

### Web-only

`机器人领域当前的发展趋势是什么？`

- Route：Web
- 本地论文 Evidence：0
- 确定性测试中的 Web Evidence：至少 1

### Hybrid

`从传统 RAG 到 Agentic RAG：对比 RAG、ReAct、Self-RAG，并结合 2026 年互联网资料分析最新发展。`

- Route：Hybrid
- 本地语义 live test：Knowledge >= 1
- 确定性 Web adapter：Web >= 1
- SSE 最终报告同时包含 `[Knowledge]` 与 `[Web]`

## 12. Performance Record

以下是同一进程内确定性本地 SSE E2E 的实际测量值，单位毫秒；Knowledge、Web
和 Summarizer 为内存 fake，因此用于验证埋点与相对调用边界，不代表公网性能：

| Route | Router | Knowledge | Web | Total retrieval | Total task |
|---|---:|---:|---:|---:|---:|
| Knowledge | 1.3575 | 0.0223 | — | 0.0319 | 1.4293 |
| Web | 0.0781 | — | 0.0127 | 0.0163 | 0.1207 |
| Hybrid | 0.0694 | 0.0141 | 0.0090 | 0.0307 | 0.1215 |

生产事件记录相同字段；真实第三方 Web/LLM 延迟需在明确授权后由
`backend/scripts/run_v03_acceptance.py` 采集，本文未伪造该数据。

## 13. Git

开发分支：`feature/semantic-rag-router`。当前阶段不合并 `main`，也不创建
`v0.3-semantic-rag-router` tag，等待 Code Review。

```text
* test: cover routed SSE end-to-end flow
* test: add semantic retrieval and routing regression tests
* feat: expose retrieval decisions through SSE
* feat: route research tasks across knowledge and web
* fix: use Loguru formatting placeholders
* feat: add knowledge relevance gating
* feat: add knowledge catalog and retrieval router
* feat: integrate HelloAgents semantic RAG backend
* refactor: introduce knowledge backend abstraction
* (tag: v0.2-knowledge-rag) V0.2 baseline
```

文档提交完成后，最终哈希和完整 `git log --oneline --decorate --graph` 以仓库为准。

## 14. Known Limitations

- 无 BM25、RRF、Reranker、Reflection Agent 或自动研究循环。
- 无 RAGAS/DeepEval、Langfuse、认证、数据库、任务队列或前端文件上传。
- 默认本地模型首次使用需要下载，CPU 冷启动明显慢于索引热查询。
- Threshold 与当前模型/语料绑定；替换模型或语料后必须重新校准。
- HelloAgents 0.2.9 自带 Qdrant adapter 的版本兼容性和 local persistence 接口有限，
  因而默认仍使用项目的 FAISS persistence。
- 真实第三方 LLM/Tavily 全链路尚未运行；需要用户明确授权外发查询后执行。

